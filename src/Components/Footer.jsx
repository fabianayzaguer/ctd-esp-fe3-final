const Footer = () => {
  return (
    <footer>
      <p>Powered by</p>
      <img src={`/images/DH.png`} alt={100} style={{ width: 'auto', height: 'auto'}}/>
    </footer>
  );
};

export default Footer;
